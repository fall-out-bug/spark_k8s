# S3 credentials secret
# IMPORTANT: Replace with actual credentials or use sealed secrets in production
apiVersion: v1
kind: Secret
metadata:
  name: s3-credentials
  namespace: spark
type: Opaque
stringData:
  access-key: "minioadmin"
  secret-key: "minioadmin"
---
# JDBC credentials for databases (used by notebooks)
apiVersion: v1
kind: Secret
metadata:
  name: jdbc-credentials
  namespace: spark
type: Opaque
stringData:
  postgres-user: "spark"
  postgres-password: "spark"
