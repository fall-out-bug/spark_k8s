
/.claude/
.claude/agents
.claude/skills
# Common local artifacts
.coverage
credentials.yaml
/.cursor/
.cursor/agents
/.cursorrules
.cursor/skills
docs/workstreams/TEMPLATE.md
.DS_Store
.env
.env.*.local
.env.local
.env.template
/hooks/   # Root-level hooks only; scripts/hooks/ is tracked
hooks/pre-push
hooks/hooks
htmlcov/
*.key
# Load test outputs
*.local.bak
# Local Cursor/IDE artifacts
*.log
/notifications/
*.pem
.prompts
__pycache__/
.pytest_cache/
quality-gate.toml
# SDP
# SDP artifacts (local-only, from submodule)
.sdp/.git
.sdp/config.yml
# SDP local backups
# SDP runtime artifacts (local-only)
.secrets/
# Secrets and credentials (NEVER commit these)
*-secrets.yaml
secrets.yaml
# Beads tracker (FROZEN per AGENTS.md — read-only local state, large dolt DB)
.beads/
# Build artifacts (spark dist)
dist/

# Demo screencast (local only)
docs/guides/ru/demo-screencast-guide.md

# Generated demo video artifacts (mp4/mp3/srt/logs, ~90MB — regenerated by scripts/seed-demo-data.sh pipeline)
assets/demo_video_generated/

# Orphaned Maven pom (spark-custom uses inline poms; deps/ belongs to docker/spark-4.1/deps/)
docker/spark-custom/deps/

# Test artifacts
tests/results/e2e-failed.txt
tests/results/failed.txt
tests/results/junit/
tests/results/load-test-*.csv
tests/results/scenario-*.json
*.tmp
tmp/
values-secrets.yaml
tests/observability/.pf/
