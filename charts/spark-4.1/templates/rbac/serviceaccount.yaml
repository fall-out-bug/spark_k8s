{{- if .Values.rbac.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.rbac.serviceAccountName | default "spark-41" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: spark-connect
    chart: spark-4.1
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- end }}
