{{- if and .Values.hiveMetastore.enabled .Values.hiveMetastore.postgresql.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "spark-standalone.fullname" . }}-postgresql-metastore-credentials
  labels:
    app: postgresql-metastore
    {{- include "spark-standalone.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_DB: {{ .Values.hiveMetastore.postgresql.database | quote }}
  POSTGRES_USER: {{ .Values.hiveMetastore.postgresql.username | quote }}
  POSTGRES_PASSWORD: {{ .Values.hiveMetastore.postgresql.password | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "spark-standalone.fullname" . }}-postgresql-metastore-data
  labels:
    app: postgresql-metastore
    {{- include "spark-standalone.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.hiveMetastore.postgresql.persistence.storageClass }}
  storageClassName: {{ .Values.hiveMetastore.postgresql.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.hiveMetastore.postgresql.persistence.size }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.hiveMetastore.postgresql.host }}
  labels:
    app: postgresql-metastore
    {{- include "spark-standalone.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgresql-metastore
      {{- include "spark-standalone.selectorLabels" . | nindent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: postgresql-metastore
        {{- include "spark-standalone.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- include "spark-standalone.podSecurityContext" . | nindent 8 }}
      containers:
      - name: postgresql
        securityContext:
          {{- include "spark-standalone.containerSecurityContext" . | nindent 10 }}
        image: "postgres:16-alpine"
        imagePullPolicy: IfNotPresent
        envFrom:
        - secretRef:
            name: {{ include "spark-standalone.fullname" . }}-postgresql-metastore-credentials
        ports:
        - name: postgres
          containerPort: {{ .Values.hiveMetastore.postgresql.port }}
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: data
        {{- if .Values.hiveMetastore.postgresql.persistence.enabled }}
        persistentVolumeClaim:
          claimName: {{ include "spark-standalone.fullname" . }}-postgresql-metastore-data
        {{- else }}
        emptyDir: {}
        {{- end }}
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.hiveMetastore.postgresql.host }}
  labels:
    app: postgresql-metastore
    {{- include "spark-standalone.labels" . | nindent 4 }}
spec:
  selector:
    app: postgresql-metastore
    {{- include "spark-standalone.selectorLabels" . | nindent 4 }}
  ports:
  - name: postgres
    port: {{ .Values.hiveMetastore.postgresql.port }}
    targetPort: postgres
{{- end }}
