---
ws_id: 027-03
feature: F27
status: backlog
size: SMALL
project_id: 00
github_issue: null
assignee: null
depends_on: ["027-01"]
---

## WS-027-03: Split Oversized Python Scripts + Add Pre-commit

### Goal

**What must WORK after completing this WS:**
- All Python scripts ≤ 200 LOC
- `.pre-commit-config.yaml` with ruff, black, mypy, shellcheck, helm-lint
- Pre-commit hooks run on `git commit`

**Acceptance Criteria:**
- [ ] AC1: `examples/iceberg/iceberg_examples.py` (395 LOC) split into modules
- [ ] AC2: `scripts/rightsizing_calculator.py` (322 LOC) split into modules
- [ ] AC3: `examples/gpu/gpu_operations_notebook.py` (249 LOC) split into modules
- [ ] AC4: `.pre-commit-config.yaml` created with hooks: ruff, black, mypy, shellcheck, helm-lint, trailing-whitespace, end-of-file-fixer
- [ ] AC5: `pre-commit run --all-files` passes (or known exclusions)
- [ ] AC6: `wc -l **/*.py` shows 0 non-test Python files > 200 LOC

### Files Changed

- `examples/iceberg/` (SPLIT into modules)
- `scripts/rightsizing_calculator.py` (SPLIT)
- `examples/gpu/` (SPLIT into modules)
- `.pre-commit-config.yaml` (NEW)
