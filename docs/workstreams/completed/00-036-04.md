---
ws_id: 00-036-04
feature: 036
status: completed
size: SMALL
project_id: 00
depends_on: ["00-036-01"]
---

# 00-036-04: Rename kubernetes → kubernetes

Feature: F036 Chart Refactor
Phase: 2
Status: Backlog

## Goal

Переименовать `kubernetes` в `kubernetes` для единообразия нейминга.
Шаблон: `templates/spark-k8s-native.yaml` → `templates/kubernetes/submitter.yaml`.
Values: `kubernetes:` → `kubernetes:`.

## Scope Files

- charts/spark-3.5/templates/spark-k8s-native.yaml (rename → templates/kubernetes/submitter.yaml)
- charts/spark-3.5/templates/rbac.yaml (update condition)
- charts/spark-3.5/values.yaml (kubernetes → kubernetes)

## Dependencies

- 00-036-01: Baseline evidence

## Acceptance Criteria

- [ ] `grep -r kubernetes charts/spark-3.5/` returns 0 matches
- [ ] `charts/spark-3.5/values.yaml` содержит `kubernetes: { enabled: false, ... }`
- [ ] `helm template test charts/spark-3.5 --set kubernetes.enabled=true` рендерит submitter Deployment
- [ ] `helm template test charts/spark-3.5` (defaults) НЕ рендерит kubernetes resources
- [ ] `helm lint charts/spark-3.5` exit code 0
- [ ] diff baseline k8s-native snapshot vs current — только переименование values path, rendered resources identical по структуре
