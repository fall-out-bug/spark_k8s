---
ws_id: 00-036-06
feature: 036
status: completed
size: LARGE
project_id: 00
depends_on: ["00-036-05"]
---

# 00-036-06: Update Presets, Demo Scripts, DAGs

Feature: F036 Chart Refactor
Phase: 3
Status: Completed

## Goal

Обновить все presets, demo scripts и DAGs для нового нейминга.
Presets: `standalone.airflow.*` → `airflow.*`, `sparkStandalone.*` → `standalone.*`.
Demo scripts: обновить hardcoded resource names.
DAGs: заменить hardcoded `spark-infra-spark-standalone-master` на env/template.

## Scope Files

- charts/spark-3.5/presets/demo-full-spark-infra.yaml
- charts/spark-3.5/presets/spark-infra-minimal.yaml
- charts/spark-3.5/presets/tuning/*.yaml
- charts/spark-3.5/presets/openshift/*.yaml
- charts/spark-3.5/presets/scenarios/*.yaml
- charts/spark-3.5/values-*.yaml (all values overlay files)
- charts/spark-3.5/airflow-*.yaml (all scenario files)
- charts/spark-3.5/jupyter-*.yaml
- charts/spark-3.5/dags/*.py (moved from subchart)
- charts/spark-3.5/dags/spark_jobs/*.py
- scripts/restore-demo.sh
- scripts/deploy-demo-minikube.sh
- scripts/check-demo-health.sh
- scripts/lib/check-dangerous-patterns.sh
- scripts/upload-spark-jobs-to-minio.sh

## Dependencies

- 00-036-05: Old code fully removed

## Acceptance Criteria

- [ ] `grep -r "sparkStandalone" charts/spark-3.5/presets/` returns 0 matches
- [ ] `grep -r "sparkK8sNative" charts/spark-3.5/presets/` returns 0 matches
- [ ] `grep -r "standalone\.airflow" charts/spark-3.5/presets/` returns 0 matches (airflow is top-level)
- [ ] `demo-full-spark-infra.yaml` содержит `standalone: { enabled: true }` и `airflow: { enabled: true }` как отдельные top-level keys
- [ ] `helm template spark-infra charts/spark-3.5 -f presets/demo-full-spark-infra.yaml --set global.s3.accessKey=x --set global.s3.secretKey=x --set spark-base.postgresql.auth.password=x` exit code 0
- [ ] Rendered demo template содержит: standalone-master, standalone-worker, airflow-webserver, airflow-scheduler, jupyter, history, metastore, minio, postgresql
- [ ] DAGs НЕ содержат hardcoded `spark-infra-spark-standalone-master` — используют env var или configurable parameter
- [ ] `scripts/restore-demo.sh` ссылается на новые имена ресурсов
- [ ] `scripts/check-demo-health.sh` проверяет компоненты с новыми labels
- [ ] Все скрипты проходят bash -n (syntax check)
