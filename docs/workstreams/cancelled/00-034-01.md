---
ws_id: 00-034-01
feature: 034
status: cancelled
cancelled_reason: Superseded by F35 (WS-035-06 covers same scope)
size: MEDIUM
project_id: 00
depends_on: []
---

## WS-00-034-01: Test Matrix — Image Pyramid + get_runtime_image

### 🎯 Goal

**What must WORK after completing this WS:**
- `get_runtime_image(spark_version, gpu, iceberg)` возвращает образ из пирамиды по dimensions
- Все 4 варианта (baseline, iceberg, gpu, gpu-iceberg) маппятся на образы

**Acceptance Criteria:**
- [ ] AC1: Пирамида образов задокументирована в `docs/drafts/idea-test-matrix-tdd.md`
- [ ] AC2: `get_runtime_image` учитывает gpu=true → -gpu suffix
- [ ] AC3: `get_runtime_image` учитывает iceberg=true → -iceberg suffix
- [ ] AC4: Матрица 3.5.7/3.5.8/4.1.0/4.1.1 × 4 вариантов = 16 образов (или skip при отсутствии)

**⚠️ WS is NOT complete until Goal is achieved (all AC ✅).**

### Red-Green-Refactor

- **Red:** gpu=true сценарий получает baseline образ → тест падает (нет RAPIDS)
- **Green:** get_runtime_image возвращает -gpu образ
- **Refactor:** Вынести маппинг в отдельную функцию/таблицу

### Input Files

- `tests/run-matrix.sh` (get_runtime_image)
- `docs/drafts/idea-test-matrix-tdd.md`
