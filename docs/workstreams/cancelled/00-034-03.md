---
ws_id: 00-034-03
feature: 034
status: cancelled
cancelled_reason: Superseded by F35 (WS-035-02..05 covers deploy/smoke/e2e/load)
size: MEDIUM
project_id: 00
depends_on: ["00-034-01"]
---

## WS-00-034-03: Test Matrix — Full Critical Path (Deploy+Smoke+E2E+Load+Metrics)

### 🎯 Goal

**What must WORK after completing this WS:**
- Каждый тест проходит все уровни: Deploy → Smoke → E2E → Load
- Metrics: Event logs → History Server (опционально на первом этапе)

**Acceptance Criteria:**
- [ ] AC1: run-matrix all (smoke+e2e+load) для одного сценария — все 3 проходят
- [ ] AC2: S3_ENDPOINT передаётся на всех уровнях (event log config)
- [ ] AC3: Load test — только S3, без in-memory fallback
- [ ] AC4: Event logs включены, MinIO init создаёт spark-logs/4.1/events

**⚠️ WS is NOT complete until Goal is achieved (all AC ✅).**

### Red-Green-Refactor

- **Red:** Load без S3 → fail (no fallback)
- **Green:** Per-file parquet read, S3 config
- **Refactor:** Вынести _list_parquet_paths в отдельный модуль

### Input Files

- `tests/run-matrix.sh`
- `tests/scripts/nyc_taxi_pipeline.py`
- `charts/spark-base/templates/minio.yaml`
