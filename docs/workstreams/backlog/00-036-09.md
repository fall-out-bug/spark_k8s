---
ws_id: 00-036-09
feature: 036
status: backlog
size: MEDIUM
project_id: 00
depends_on: ["00-036-07", "00-036-08"]
---

# 00-036-09: Update Tests

Feature: F036 Chart Refactor
Phase: 4
Status: Backlog

## Goal

Обновить все тесты для нового нейминга. Тесты ДОЛЖНЫ вызывать `helm template` /
`helm lint` / script execution. Запрещены тесты с `Path.exists()` или `"keyword" in text`
как единственной проверкой.

## Scope Files

- tests/integration/test_run_matrix.py
- tests/integration/test_demo_preset_guard.py
- tests/demo-values.yaml
- scripts/tests/e2e/fixtures_standalone.py

## Dependencies

- 00-036-07: Matrix updated
- 00-036-08: Scripts updated

## Acceptance Criteria

- [ ] `grep -r "sparkStandalone\|sparkK8sNative" tests/` returns 0 matches
- [ ] `grep -r "spark-standalone" tests/integration/` returns 0 matches
- [ ] test_run_matrix.py: тест test_run_matrix_standalone_scenario_no_airflow вызывает `helm template` и asserts no airflow in output
- [ ] test_run_matrix.py: тест для kubernetes mode вызывает `helm template` и asserts submitter in output
- [ ] test_demo_preset_guard.py: обновлён для `standalone:` и `airflow:` как top-level keys
- [ ] `pytest tests/integration/ -v` — все тесты pass
- [ ] Ни один тест не использует `assert Path(...).exists()` как единственное условие
- [ ] Ни один тест не использует `assert "keyword" in file.read_text()` как единственное условие
