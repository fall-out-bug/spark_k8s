---
ws_id: 00-036-05
feature: 036
status: backlog
size: MEDIUM
project_id: 00
depends_on: ["00-036-02", "00-036-03"]
---

# 00-036-05: Remove sparkStandalone Duplicate, Unify Values

Feature: F036 Chart Refactor
Phase: 3
Status: Backlog

## Goal

Удалить `templates/spark-standalone.yaml` (parent duplicate) и секцию `sparkStandalone:`
из values.yaml. После 036-02 standalone живёт в `templates/standalone/`.
Удалить subchart directory `charts/spark-3.5/charts/spark-standalone/` полностью.
Удалить `charts/spark-3.5/charts/spark-standalone-0.1.0.tgz`.

## Scope Files

- charts/spark-3.5/templates/spark-standalone.yaml (delete)
- charts/spark-3.5/values.yaml (remove sparkStandalone section)
- charts/spark-3.5/charts/spark-standalone/ (delete entire directory)
- charts/spark-3.5/charts/spark-standalone-0.1.0.tgz (delete)

## Dependencies

- 00-036-02: Standalone flattened into parent
- 00-036-03: Airflow extracted into parent

## Acceptance Criteria

- [ ] `grep -r sparkStandalone charts/spark-3.5/` returns 0 matches
- [ ] `ls charts/spark-3.5/charts/spark-standalone/` fails (directory deleted)
- [ ] `ls charts/spark-3.5/charts/spark-standalone-0.1.0.tgz` fails (file deleted)
- [ ] `grep -r "spark-standalone" charts/spark-3.5/Chart.yaml` returns 0 matches
- [ ] `helm template test charts/spark-3.5 --set standalone.enabled=true --set airflow.enabled=true` рендерит все resources для demo mode
- [ ] `helm lint charts/spark-3.5` exit code 0
- [ ] Symlink `charts/spark-standalone` at repo root — удалить или перенаправить
