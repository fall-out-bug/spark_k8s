---
ws_id: 00-036-07
feature: 036
status: backlog
size: MEDIUM
project_id: 00
depends_on: ["00-036-05"]
---

# 00-036-07: Update run-matrix, test-matrix, shared-infra-values

Feature: F036 Chart Refactor
Phase: 4
Status: Backlog

## Goal

Обновить матричную инфраструктуру для нового нейминга.
run-matrix.sh: убрать костыль `standalone.enabled=false`, использовать `standalone.*` и `kubernetes.*`.
test-matrix.yaml: обновить helm_values во всех 320 сценариях.
shared-infra-values.yaml: обновить если нужно.

## Scope Files

- scripts/run-matrix.sh
- scripts/run-matrix-32.sh
- scripts/run-matrix-96.sh
- scripts/run-matrix-320.sh
- tests/test-matrix.yaml
- tests/shared-infra-values.yaml

## Dependencies

- 00-036-05: Old values paths removed

## Acceptance Criteria

- [ ] `grep -r "sparkStandalone" scripts/run-matrix*.sh` returns 0 matches
- [ ] `grep -r "sparkK8sNative" scripts/run-matrix*.sh` returns 0 matches
- [ ] `grep -r "standalone.enabled=false" scripts/run-matrix*.sh` returns 0 matches (костыль удалён)
- [ ] `grep -r "sparkStandalone" tests/test-matrix.yaml` returns 0 matches
- [ ] `grep -r "sparkK8sNative" tests/test-matrix.yaml` returns 0 matches
- [ ] test-matrix.yaml: standalone сценарии содержат `standalone.enabled=true`
- [ ] test-matrix.yaml: k8s-native сценарии содержат `kubernetes.enabled=true`
- [ ] `./scripts/run-matrix-32.sh --dry-run all` exit code 0, все 32 сценария проходят
- [ ] `./scripts/run-matrix.sh --filter "gpu=false,platform=k8s" --dry-run deploy` — 96 сценариев проходят
- [ ] run-matrix.sh: DEPLOY_MODE определяется из connect/standalone/kubernetes flags
- [ ] Все скрипты проходят bash -n
