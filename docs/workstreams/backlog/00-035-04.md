---
ws_id: 00-035-04
feature: 035
status: completed
size: MEDIUM
project_id: 00
depends_on: ["00-035-01"]
---

## WS-00-035-04: E2E Level — Execute Workload

### Goal

**What must WORK after completing this WS:**
- E2E скрипт **выполняет** workload: 10K rows, aggregations, joins
- Реальный SQL/DataFrame workload
- Assert: результат корректен
- Exit 0 = E2E_SUCCESS

### Acceptance Criteria

- [x] AC1: E2E НЕ проверяет Path.exists() — только выполняет workload
- [x] AC2: Workload: aggregations (GroupBy), joins
- [x] AC3: Assert: результат содержит ожидаемые данные
- [x] AC4: Интеграция с run-matrix: вызов после smoke

### Input Files

- `scripts/tests/e2e/scripts/e2e_10k_agg_join.py`
- `scripts/tests/e2e/run-e2e-against-release.sh`

---

## Execution Report

**Date:** 2026-03-04
**Status:** PASS

### Deliverables

| Artifact | Path |
|----------|------|
| E2E workload script | `scripts/tests/e2e/scripts/e2e_10k_agg_join.py` |
| E2E runner script | `scripts/tests/e2e/run-e2e-against-release.sh` |
| run-matrix integration | `scripts/run-matrix.sh` (e2e level after smoke) |
| Tests | `tests/integration/test_run_matrix.py` |

### Evidence

- `e2e_10k_agg_join.py`: 10K rows, groupBy(bucket).agg(count, sum), inner join, asserts, prints E2E_SUCCESS
- `run-e2e-against-release.sh`: NAMESPACE, kubectl cp, kubectl exec spark-submit
- `run-matrix.sh`: calls `run-e2e-against-release.sh` when e2e in levels
- Tests: `test_e2e_workload_executes_no_path_exists`, `test_e2e_against_release_script_exists`
- Local run: `python3 e2e_10k_agg_join.py` → E2E_SUCCESS

### Tests Run

```
pytest tests/integration/test_run_matrix.py -v --tb=short
8 passed in 1.24s
```
