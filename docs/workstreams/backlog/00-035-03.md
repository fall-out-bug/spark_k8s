---
ws_id: 00-035-03
feature: 035
status: completed
size: MEDIUM
project_id: 00
depends_on: ["00-035-01"]
---

## WS-00-035-03: Smoke Level — Execute Workload

### Goal

**What must WORK after completing this WS:**
- Smoke скрипт **выполняет** workload: 1K rows, count, filter
- Вызов Spark Connect API или spark-submit
- Assert: результат содержит ожидаемое число строк
- Exit 0 = SMOKE_SUCCESS

### Acceptance Criteria

- [x] AC1: Smoke НЕ проверяет Path.exists() — только выполняет workload
- [x] AC2: Workload: создание DataFrame (1K rows), count(), filter
- [x] AC3: Assert: count совпадает с ожидаемым
- [x] AC4: Интеграция с run-matrix: вызов после deploy

### Forbidden

- Проверка существования скрипта вместо его выполнения
- `"keyword" in content` как доказательство

### Input Files

- `scripts/tests/smoke/run-smoke-against-release.sh`
- `scripts/tests/smoke/scripts/smoke_1k_count_filter.py`

---

## Execution Report

**Date:** 2026-03-04
**Status:** PASS

### Deliverables

| Artifact | Path |
|----------|------|
| Smoke workload script | `scripts/tests/smoke/scripts/smoke_1k_count_filter.py` |
| Smoke runner script | `scripts/tests/smoke/run-smoke-against-release.sh` |
| run-matrix integration | `scripts/run-matrix.sh` (smoke level after deploy) |
| Tests | `tests/integration/test_run_matrix.py` |

### Evidence

- `smoke_1k_count_filter.py`: runs `spark.range(1000)`, `count()`, `filter("id > 500")`, asserts 1000 and 499, prints SMOKE_SUCCESS
- `run-smoke-against-release.sh`: uses NAMESPACE, kubectl cp to connect pod, kubectl exec spark-submit
- `run-matrix.sh`: calls `run-smoke-against-release.sh` after deploy when smoke in levels
- Tests: `test_smoke_workload_executes_no_path_exists`, `test_smoke_against_release_script_exists` — no Path.exists()
- Local run: `python3 smoke_1k_count_filter.py` → SMOKE_SUCCESS

### Tests Run

```
pytest tests/integration/ -v --tb=short
38 passed in 6.56s
```
