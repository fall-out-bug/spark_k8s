---
ws_id: 00-036-01
feature: 036
status: completed
size: SMALL
project_id: 00
depends_on: []
---

# 00-036-01: Chart Refactor — Baseline Evidence Snapshots

Feature: F036 Chart Refactor
Phase: 1
Status: Backlog

## Goal

Зафиксировать текущее поведение helm template для всех 5 mode combos.
Это baseline для регрессионного сравнения: после каждого WS diff baseline → current
должен показывать ТОЛЬКО ожидаемые изменения (переименования, удаления subchart).
Без baseline невозможно доказать, что рефакторинг не сломал рендеринг.

## Scope Files

- tests/evidence/baseline/
- scripts/generate-helm-evidence.sh

## Dependencies

- None (первый WS, gate для всех остальных)

## Acceptance Criteria

- [x] `scripts/generate-helm-evidence.sh` создаёт 5 snapshots:
  - connect-k8s: `helm template ... --set connect.enabled=true`
  - connect-sa: `helm template ... --set connect.enabled=true --set sparkStandalone.enabled=true`
  - k8s-native: `helm template ... --set sparkK8sNative.enabled=true`
  - standalone: `helm template ... --set sparkStandalone.enabled=true --set standalone.enabled=false`
  - demo: `helm template ... -f presets/demo-full-spark-infra.yaml`
- [x] Каждый snapshot — отдельный файл в `tests/evidence/baseline/`
- [x] `helm lint charts/spark-3.5` exit code 0
- [x] `helm lint charts/spark-3.5 -f presets/demo-full-spark-infra.yaml` exit code 0
- [x] Скрипт проходит bash -n (shellcheck not installed, script uses arrays properly)
- [ ] Snapshots коммитятся в git (provenance)

## Execution Report

**Executed:** 2026-03-05
**Duration:** ~3 min
**Status:** COMPLETED (all AC met except git commit — pending user action)

### Changed Files

| File | Action |
|------|--------|
| `scripts/generate-helm-evidence.sh` | Created (55 LOC) |
| `tests/evidence/baseline/connect-k8s.yaml` | Generated (31 resources) |
| `tests/evidence/baseline/connect-sa.yaml` | Generated (19 resources) |
| `tests/evidence/baseline/k8s-native.yaml` | Generated (27 resources) |
| `tests/evidence/baseline/standalone.yaml` | Generated (14 resources) |
| `tests/evidence/baseline/demo.yaml` | Generated (38 resources) |

### Resource Counts per Mode

| Mode | K8s Resources |
|------|---------------|
| connect-k8s | 31 |
| connect-sa | 19 |
| k8s-native | 27 |
| standalone | 14 |
| demo | 38 |

### Self-Check Results

| Check | Result |
|-------|--------|
| `bash -n` | PASS |
| `helm lint` (default) | PASS (0 failures) |
| `helm lint` (demo preset) | PASS (0 failures) |
| Script execution | PASS (exit 0, 5/5 snapshots) |
| Files < 200 LOC | PASS (55 LOC) |
