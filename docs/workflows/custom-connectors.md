# Custom Connectors

## Data Source API

Implement `DataSourceV2` for custom sources/sinks.

## Kafka Connector

Use `spark-sql-kafka` for Kafka source/sink.

## S3 Connector

`s3a://` with Hadoop AWS. Configure credentials via IAM or keys.

## Custom Sink

Implement `StreamWriteSupport` for streaming sinks.
