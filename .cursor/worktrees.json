{
  "setup_commands_unix": [
    "poetry install --no-interaction --quiet 2>/dev/null || echo 'Poetry not configured'",
    "echo '✅ Worktree ready'"
  ],
  "setup_commands_windows": [
    "poetry install --no-interaction --quiet 2>nul || echo Poetry not configured",
    "echo Worktree ready"
  ],
  "description": "Template for project worktree setup. Copy and customize for your project."
}
